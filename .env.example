# Default allowed domains — comma separated. Use * to allow all.
ALLOWED_DOMAINS=*

# Secret token required to download (set in GitHub repo secrets too)
DOWNLOAD_TOKEN=

# Optional defaults (uncomment to override)
# SPLIT_SIZE=90
# COMMIT_MSG=chore: download files
# MODE=download
